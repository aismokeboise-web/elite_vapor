EmpireVapor123##

sudo apt update
sudo apt install -y git build-essential nginx postgresql postgresql-contrib

sudo mkdir -p /var/www/empire
sudo chown -R root:root /var/www/empire

cd /var/www/empire
git clone YOUR_GITHUB_REPO_URL .

CREATE DATABASE elite_prod;
CREATE USER elite_user WITH PASSWORD 'STRONG_PASSWORD_HERE';
GRANT ALL PRIVILEGES ON DATABASE elite_prod TO elite_user;
\q