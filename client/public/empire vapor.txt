EmpireVapor123##

sudo apt update
sudo apt install -y git build-essential nginx postgresql postgresql-contrib

sudo mkdir -p /var/www/empire
sudo chown -R root:root /var/www/empire

cd /var/www/empire
git clone YOUR_GITHUB_REPO_URL .

CREATE DATABASE empire_prod;
CREATE USER empire_user WITH PASSWORD 'password';
GRANT ALL PRIVILEGES ON DATABASE empire_prod TO empire_user;
\q

cd /var/www/empire/server
nano .env.production

git config --global user.name "Empire Web"
git config --global user.email "aismokeboise@gmail.com"